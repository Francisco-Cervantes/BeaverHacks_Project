routes/ Directory

This directory is for additional API route modules.

Currently empty - all routes are defined in main.py.

Purpose:
- Organize API endpoints into separate modules as the application grows
- Keep main.py focused on app setup
- Future: user_routes.py, meal_routes.py, etc.